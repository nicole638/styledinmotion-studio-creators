# Public Supabase credentials (safe to expose; same as web/ + iOS app)
NEXT_PUBLIC_SUPABASE_URL=https://rghlcnrttvlvphzahudf.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=...

# Server-only — required for invite consumption + future admin actions
# NEVER expose this with a NEXT_PUBLIC_ prefix.
SUPABASE_SERVICE_ROLE_KEY=...

# Backend Hono server (existing, used for productInfo URL scraping in
# Phase 1B closet flow). Leave blank in Phase 1A.
NEXT_PUBLIC_BACKEND_URL=

# Site URL (used for auth redirects). Local: http://localhost:3001
# Production: https://studio.styledinmotion.studio
NEXT_PUBLIC_SITE_URL=http://localhost:3001
