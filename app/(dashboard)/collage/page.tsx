import { ComingSoon } from "../_components/ComingSoon";

export const metadata = { title: "Collage" };

export default function CollagePage() {
  return (
    <ComingSoon
      title="Build collages."
      description="The same templates as the iOS app — Style Journal, Editorial, Grid, Editorial Cover — with a desktop canvas and mouse-friendly drag-and-drop."
    />
  );
}
