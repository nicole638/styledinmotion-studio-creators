import { ComingSoon } from "../_components/ComingSoon";

export const metadata = { title: "Looks" };

export default function LooksPage() {
  return (
    <ComingSoon
      title="Compose your looks."
      description="Upload a cover photo from your desktop, multi-select pieces from your closet, write a caption, and publish — or save a draft and finish later."
    />
  );
}
