import { ComingSoon } from "../../_components/ComingSoon";

export const metadata = { title: "Add piece" };

export default function NewClosetItemPage() {
  return (
    <ComingSoon
      title="Add a piece."
      description="The URL paste flow ships in the next batch. For now, add pieces from the iOS app and they'll show up here automatically."
    />
  );
}
